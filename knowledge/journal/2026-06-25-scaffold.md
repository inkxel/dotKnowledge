---
type: Journal
date: 2026-06-25
session: scaffold
status: complete
related: [[roadmap]]
---

# Scaffold — founding the `.knowledge` public standard repo

## Context
`.knowledge` is the universal/open layer beneath a private multi-client knowledge architecture (capsules → convergence → shared brain) being dogfooded inside a real org. Decision taken to build it public-first (see [[decisions/2026-06-25-public-first-standard]]). This session scaffolds the repo.

## What changed
- Authored `README.md` (positioning / flag) + `SPEC.md` (DRAFT / RFC v0: bundle layout, frontmatter metadata incl. classification, the `rises:` boundary, the convergence protocol, OKF conformance + extensions).
- CC-BY-4.0 LICENSE (reused from the motion.md standard precedent).
- Added this knowledge layer (build memory) via the knowledge-layer skill; trimmed the codemap/internal bits (spec repo, no source — kept it lean).

## Decisions made
- **Public-first standard** — [[decisions/2026-06-25-public-first-standard]].

## Open threads
- [ ] Decide whether the convergence protocol ships a reference runtime or stays protocol-only.
- [ ] Open an OKF-extension RFC / issue staking `BOUNDARY`/`rises:` + classification (the OKF contribution window).
- [ ] Pin the Throughline ↔ `.knowledge` relationship explicitly in both repos.
- [ ] Eject / compact-at-maturity is a Throughline feature ([[roadmap]]).

## Session close — 2026-06-25 (live + called it here)
- **Pushed public** — repo is live at **https://github.com/inkxel/dotKnowledge** (CC-BY-4.0, name locked). Carries the flag-planting README, the DRAFT SPEC, a root `CLAUDE.md` pointer, and this lean knowledge layer.
- The private architecture it underpins (refined convergence-gates model) was committed to `cosi-knowledge` `b716a4b` in the same sitting — moved from a Slack thread → committed private architecture + a public standard in one pass.
- Tucker called the work here for the day. **The flag isn't fully planted until the OKF-extension RFC/issue is opened** (staking `BOUNDARY`/`rises:` + classification in the OKF orbit — roadmap "Near"). Cheap, high-leverage next step when ready; draft-ready.

## Term resolved — `capsule` (2026-06-25)
The self-contained `.knowledge` unit is named a **capsule**. The original working term "cartridge" was dropped as too idiosyncratic; "synapse" was rejected (it names a *connection* — the convergence layer — not the discrete unit, and collides with Azure/Matrix Synapse); "bundle" and "cell" were also weighed. **Rename executed** across this repo + `cosi-knowledge`. Full rationale + candidate trail: `cosi-knowledge` decision `2026-06-25-rename-cartridge-term` (accepted).

## Related
- `SPEC.md`, `README.md`; reference engine: [Throughline](https://github.com/inkxel/throughline); private dogfood: `cosi-knowledge` (`b716a4b`).
